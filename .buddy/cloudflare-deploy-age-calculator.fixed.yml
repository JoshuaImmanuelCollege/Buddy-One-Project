- pipeline: cloudflare_deploy_age_calculator
  name: cloudflare-deploy-age-calculator
  refs:
  - refs/heads/main
  events:
  - type: PUSH
  fail_on_prepare_env_warning: true
  resources: NANO
  actions:
  - action: Purge cache at $BUDDY
    type: CLOUDFLARE
    integration: cloudflare
    zone: $BUDDY
